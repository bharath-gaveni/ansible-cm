- name: "Conditions"
  hosts: "local"
  connection: "local"
  vars:
    number:14
  tasks:
    - name: "checking whether given number is less than 10 or not"
      ansible.builtin.debug:
        msg: "Given number {{ number }} is less than 10
      when: number < 10