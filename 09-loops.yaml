- name: "checking the loop in ansible"
  hosts: "local"
  connection: "local"
  become: "yes"
  tasks:
  - name: "printing the simple loop"
    ansible.builtin.debug:
      msg: DevOps with {{ item }}
    loop:
    - AWS
    - Azure
    - GCP

