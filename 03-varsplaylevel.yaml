- name: variables play-level
  hosts: local
  connection: local
  vars:
    Role: "AWS DevOps Engineer"
    Location: "Hyderabad"
    company: "Infosys"
  tasks:
    - name: "checking the play-level variables"
      ansible.builtin.debug:
        msg: "Role is {{ Role }} , location is {{ Location }} , and company is {{ company }}"
